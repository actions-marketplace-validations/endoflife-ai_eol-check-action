{
  "name": "eol-check-action",
  "version": "1.0.0",
  "description": "GitHub Action to check runtimes against endoflife.ai",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "node src/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  },
  "license": "MIT"
}
